<%= render "shared/context_nav", title: t("activerecord.models.#{REstruturaOrganizacional.model_name.i18n_key}.other"), new_path: new_r_estrutura_organizacional_path do  %>
  <span class="breadcrumb-item active"><%= t("activerecord.models.#{REstruturaOrganizacional.model_name.i18n_key}.other") %></span>
<% end %>
<div class="card">
  <div class="card-header d-flex">
    <%= search_form_for @q, html: { class: "d-flex gap-2" , autocomplete: "off"} do |f| %>
      <%= f.button(type: 'submit', class: 'btn btn-info') { tag.i(class: 'ph-magnifying-glass') } %>
      <%= link_to  tag.i(class: 'ph-x-circle'), r_estruturas_organizacionais_path, class: "btn btn-light" if params[:q] %>
    <% end %>      
  </div>
  <div class="card-body">
    <%= render "shared/notice" %>
    <div class= "table-responsive">
      <table class="table table-sm table-bordered table-striped">        
        <thead>
          <th class="text-center col-1">Ações</th>
          <tr>
            
              
                <th><%= sort_link(@q, :i_ambiente_predial, t("activerecord.attributes.r_estrutura_organizacional.i_ambiente_predial")) %></th>
              
              
                <th><%= sort_link(@q, :r_organizacional, t("activerecord.attributes.r_estrutura_organizacional.r_organizacional")) %></th>
              
            
          </tr>
        </thead>
        <tbody>
          <% @r_estruturas_organizacionais.each do |r_estrutura_organizacional| %>
            <tr>
              <td class="text-center">
                <div class="dropdown-toggle">
                  <a href="#" class="text-body dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="ph-gear"></i>
                  </a>
                  <div class="dropdown-menu" style="">
                    <div class="dropdown-header">Opções</div>
                    <%= link_to edit_r_estrutura_organizacional_path(r_estrutura_organizacional), class: "dropdown-item" do %>
                      <i class="ph-pen me-2"></i> Editar
                    <% end %>
                    <%= form_for r_estrutura_organizacional, method: :delete do |f| %>
                      <a class="dropdown-item hand-pointer " data-confirm-delete="<%= r_estrutura_organizacional_path(r_estrutura_organizacional) %>" data-csrf-token="<%= form_authenticity_token %>">
                        <i class="ph-trash-simple me-2"></i>Excluir
                      </a>
                    <% end %>
                  </div>
                </div>
              </td>
                
                  <td><%= r_estrutura_organizacional.i_ambiente_predial %></td>
                
                
                  <td><%= r_estrutura_organizacional.r_organizacional %></td>
                
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="card-footer">
    <%= render "shared/pagy", pagy: @pagy %>
  </div>
</div>
